name: Deploy to Production

on:
  push:
    branches:
      - main

jobs:
  migrate-and-deploy:
    name: Run Migrations & Deploy
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🗄️ Install PostgreSQL client
        run: |
          sudo apt-get update
          # Remove any existing PostgreSQL client to avoid conflicts
          sudo apt-get remove -y postgresql-client postgresql-client-common postgresql-16 postgresql-client-16 2>/dev/null || true
          # Install PostgreSQL 15 client (matches Supabase server version)
          sudo apt-get install -y wget ca-certificates lsb-release gnupg
          # Add PostgreSQL official APT repository with modern GPG key handling
          sudo mkdir -p /etc/apt/keyrings
          wget --quiet -O - https://www.postgresql.org/media/keys/ACCC4CF8.asc | sudo gpg --dearmor -o /etc/apt/keyrings/postgresql.gpg
          echo "deb [signed-by=/etc/apt/keyrings/postgresql.gpg] http://apt.postgresql.org/pub/repos/apt $(lsb_release -cs)-pgdg main" | sudo tee /etc/apt/sources.list.d/pgdg.list
          sudo apt-get update
          sudo apt-get install -y postgresql-client-15
          # Ensure PostgreSQL 15 binaries are in PATH
          echo "/usr/lib/postgresql/15/bin" >> $GITHUB_PATH
          # Verify pg_dump is available and show version
          if [ -f "/usr/lib/postgresql/15/bin/pg_dump" ]; then
            /usr/lib/postgresql/15/bin/pg_dump --version
          elif command -v pg_dump >/dev/null 2>&1; then
            pg_dump --version
          else
            echo "Warning: pg_dump not found, but continuing..."
          fi

      - name: 🔍 Check for pending migrations
        id: check_migrations
        env:
          SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
        run: |
          chmod +x scripts/check-pending-migrations.sh
          if ./scripts/check-pending-migrations.sh; then
            echo "has_pending=true" >> $GITHUB_OUTPUT
            echo "📋 Pending migrations detected - backup will be created"
          else
            echo "has_pending=false" >> $GITHUB_OUTPUT
            echo "✨ No pending migrations - skipping backup"
          fi

      - name: 💾 Backup database (if migrations pending)
        if: steps.check_migrations.outputs.has_pending == 'true'
        env:
          SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_RUN_ID: ${{ github.run_id }}
          GITHUB_REF: ${{ github.ref }}
        run: |
          chmod +x scripts/backup-with-metadata.sh
          mkdir -p database/backups
          ./scripts/backup-with-metadata.sh database/backups
          echo "📦 Backup created before migrations"

      - name: 📦 Upload backup artifact
        if: steps.check_migrations.outputs.has_pending == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: db-backup-${{ github.run_id }}-${{ github.sha }}
          path: |
            database/backups/backup_*.sql
            database/backups/backup_*.metadata.json
          retention-days: 90
          if-no-files-found: ignore

      - name: 🔄 Run database migrations
        env:
          SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
        run: |
          chmod +x scripts/run-migrations.sh
          ./scripts/run-migrations.sh

      - name: 🚀 Deploy to Vercel
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          vercel-args: '--prod'

      - name: ✅ Deployment complete
        run: |
          echo "🎉 Deployment completed successfully!"
          if [ "${{ steps.check_migrations.outputs.has_pending }}" == "true" ]; then
            echo "💾 Database backup created and stored as artifact"
          fi
          echo "📊 Migrations applied"
          echo "🌐 Application deployed to Vercel"

